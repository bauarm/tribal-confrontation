export function measureDistance(aPoint, bPoint) {
    const result = Math.sqrt(((aPoint[0] - bPoint[0]) ** 2) + ((aPoint[1] - bPoint[1]) ** 2));
    return result;
}
export function checkDistance() {
}
